import { BlockChangeExample } from './block-change';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '随机块状背景动画',
  level: 2,
};

export const BlockChange: DemoComponentProps = {
  catalogInfo,
  Component: BlockChangeExample,
};

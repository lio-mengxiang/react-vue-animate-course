import { DockMenuExample } from './dock-menu';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: 'Mac Menu 效果',
  level: 2,
};

export const DockMenu: DemoComponentProps = {
  catalogInfo,
  Component: DockMenuExample,
};

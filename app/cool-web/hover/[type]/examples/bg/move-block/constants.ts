import { MoveBlockExample } from './move-block';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '方格无限滚动背景',
  level: 2,
};

export const MoveBlock: DemoComponentProps = {
  catalogInfo,
  Component: MoveBlockExample,
};

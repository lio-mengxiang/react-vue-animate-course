import { SpotlightExample } from './spotlight';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '聚光格子背景',
  level: 2,
};

export const Spotlight: DemoComponentProps = {
  catalogInfo,
  Component: SpotlightExample,
};

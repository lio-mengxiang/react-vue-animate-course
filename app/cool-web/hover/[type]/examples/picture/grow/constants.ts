import { GrowExample } from './grow';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '图片自适应放大',
  level: 2,
};

export const Grow: DemoComponentProps = {
  catalogInfo,
  Component: GrowExample,
};

import { ImageExample } from './image-card';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '3D 卡片',
  level: 2,
};

export const Image: DemoComponentProps = {
  catalogInfo,
  Component: ImageExample,
};

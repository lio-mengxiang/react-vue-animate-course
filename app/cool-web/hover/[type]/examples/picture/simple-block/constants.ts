import { SimpleBlockExample } from './simpl-block';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '格子特效',
  level: 2,
};

export const SimpleBlock: DemoComponentProps = {
  catalogInfo,
  Component: SimpleBlockExample,
};

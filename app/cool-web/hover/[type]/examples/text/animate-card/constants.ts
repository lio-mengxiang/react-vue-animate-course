import { AnimateCardExample } from './animate-card';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '文字说明动画',
  level: 2,
};

export const AnimateCard: DemoComponentProps = {
  catalogInfo,
  Component: AnimateCardExample,
};

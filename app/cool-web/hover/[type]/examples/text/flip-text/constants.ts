import { FlipTextExample } from './flip-text';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '文字翻转',
  level: 2,
};

export const FlipText: DemoComponentProps = {
  catalogInfo,
  Component: FlipTextExample,
};

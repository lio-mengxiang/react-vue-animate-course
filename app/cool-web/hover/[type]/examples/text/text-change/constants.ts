import { TextChangeExample } from './text-change';
import { type DemoComponentProps } from '@/cool-web/interface';

export const catalogInfo = {
  text: '中间到两边文字动画',
  level: 2,
};

export const TextChange: DemoComponentProps = {
  catalogInfo,
  Component: TextChangeExample,
};
